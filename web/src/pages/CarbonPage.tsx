import { PageContainer } from "../shared/ui/PageContainer";

// 蓝碳/MRV: 蓝碳本底与核算 —— P3 仅架构占位, 按锁定策略不提前实现。
export function CarbonPage() {
  return (
    <PageContainer
      title="蓝碳 / MRV"
      subtitle="蓝碳本底核算与 MRV 证据链(架构预留, 后续阶段实现)。"
      phase="P3"
    />
  );
}
