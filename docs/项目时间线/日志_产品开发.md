
# 产品路线图

| 步骤 | 内容 | 产出 |
| --- | --- | --- |
| **① 需求与场景** | 拆解政府 vs 企业客户到底买什么、为什么买、谁付钱 | 客户画像 + 核心场景清单 |
| **② 业务流程** | 定义"数据进 → 价值出"的标准作业流程 | 端到端业务流程图 |
| **③ 产品定位** | 是否升级为"红树林数字化管理平台"及如何分层包装 | 产品定位与价值主张 |
| **④ 功能体系** | 功能模块分层（检测/分割/变化/碳汇/报告/管理） | 功能架构 + 优先级 |
| **⑤ 商业模式** | 定价、交付形态（SaaS/私有化/项目制）、客户成功 | 商业模式画布 |
| **⑥ 软件工程** | 从原型到可交付系统的技术架构演进 | 技术架构与演进路线 |
| **⑦ 售后与交付** | 实施、培训、运维、SLA、数据安全合规 | 售后体系设计 |


# steps

## 重构数据库

我想彻底重构数据库models, 以最优雅,最符合业内最佳实践的方式实现层级管理
- 重要概念:
    - 行政区划ID: 格式`市_县`(形如`湛江_徐闻`), 通过极致的低复杂度算法自动获得
    - 地块ID: 任意字符串, 在同一行政区划内具有唯一性, 支持用户手动指定, 或默认为其下属唯一`正射影像文件名`. 
        - 设计哲学: 把地块和TIFF解耦, 应对某一片种植区过大而无法被用户制作成一张TIFF的情况. 
        - `地块表`的列中至少要有:
            - TIFF ID集合: 其尺寸为[0,n]
            - `地块多边形`: 支持用户手动指定; 默认时: 当TIFF ID集合尺寸为1, 直接用那个TIFF的四角作为多边形; 当不为1则留空(等待用户指定).
			- `状态`: 无/半/全影像覆盖
    - TIFF ID: 格式`5位长度的哈希值`(基于TIFF四角坐标的哈希值, 计算前先规范为经纬坐标系和3位小数精度)
        - 设计哲学: 为了防止用户对同一个TIFF改路径/重命名, 用哈希值作为ID更好对吗?
		- `TIFF表`的列中至少要有:
			- `文件路径版本`: 是一个字典, 键是`读取日期`(格式`YYYYMMDD`), 值是完整路径(保持原始, 不要做任何转换(类似wslpath那种)). 如果要读该TIFF的路径, 默认直接读最新日期的.
			- `多源文件路径`: 是字典, 键是`文件类型`(如: `chm`、`dsm`、`dem`、`las` 等), 值是又是个字典, 内部套用上面`文件路径版本`的逻辑.
			- `状态`: 未/已推理
    - 时相ID: 格式`6位长度的时间戳`(形如`20260707`)
		- 设计哲学: 
			- 一个地块/TIFF可以有多个时相. 在指定的`地块+时相`下, 其`TIFF集合`里每个TIFF的时相必须相同, 这才符合实际对吧
			- TIFF_ID + 时相ID 唯一标识一个TIFF图像
		- 来自
	- 单木个体ID: 格式`8位哈希长度`, 每棵树全球唯一, 便于跨时相追踪
	- run_id: 格式`6位哈希长度`
- 我上面对概念的设计是否符合最佳实践, 需不需要改进
- 然后你彻底重构最佳的数据库: 需要哪些表, 每个表设置哪些列. 不要有历史包袱, 不要担心适配现有代码(未来代码还会依照数据的变化而修改呢), 不要有任何限制, 我要的是一个符合我项目初衷的数据库.

所以你为了一碟醋包了盘饺子?那还是把`tree_phase_states`表删了吧,别考虑`new`状态了. 且tree_observations里没让你做crown_area_px_real和crown_area_px_est的拆分啊,回退. 且 `area_method`怎么还变成列属性了,这不没事    找事吗,只在配置文件里添加该配置节点不就行了?简单改下吧.

  然后就要据此重构项目了,工程量对你来说吃得消吗? 能确保一步到位且前后端一切功能正常吗?  
我能给你的帮助只有: 
- 在适当的时候批准你使用沙盒外的GPU等
- 已经把`~/.4estDS`软链接到了项目的`./.4estDS`
- 供测试的数据: 多源推理时用`data/多源数据研究_徐闻`, 准备多时相的推理时用`data/TIFFs/Q12.tif`(手动指定两次时相为20260601 20260701, 且使用不同的模型,即 .4estDS/models 里的`m03`和`m07`)
- 其他需要我明确的或批准的, 实时告诉我即可

开始你的大工程吧.

### 改进

1. 你提到`做低风险过渡：location 先映射为 tract_id，region_id 尝试从路径/名称里解析 市_县`, 别做映射, 本来就要代替
2. 你提到`schema 已切到新表，并且旧表不会直接删除`, 这难道不就是所谓的`历史包袱`? 
3. 你提到`旧的函数名保留给 service/API 调用。` / `对外保留前端/API 目前依赖的字段别名，比如 acquisition_time/location/center_geo/obs_id。` / `schema 里为了保留瓦片缓存和作业历史功能，runs 实际保留了 input_path/tiles_dir，tiffs 保留了 geo_area/area_unit。` / `但 API 里暂时保留 location/acquisition_time 兼容字段，因为现有路由和表单还在用它们表达“地块/时相”的旧入参。` 这些也是历史包袱吗
4. 我当初要你做重构的工作, 就一定是别考虑去兼容旧设计/兼容前端现有的形状, 而是要改头换面, 使新代码优雅且清爽. 如有必要, 前端也改, 别客气. 你顺便审查整个项目, 找找哪些地方有冗余的/非必要的/僵尸性质的/意义不大的兜底性质的代码, 大刀阔斧地改, 并给出简单的报告落盘到一个md里(总结优化了哪些地方, 增减了多少行代码等).


## ✅ 重构监管台账页面

1. 当页面有`路网按钮`时，默认设为关闭状态。  
2. 除了`/map`页面, 每在一个path, 不管左侧的导航栏存在与否, 顶部的`红树林生态资产监管平台`右边都要带上破折号和该页的中文名.  appfooter的高度也要降低
3. 搜索框: 现有的支持地块名搜索很棒, 但可供搜索的通用地名似乎不够全, 能支持到市-县区-乡镇吗?
4. `/change?tract_id=Q12`页面:
  - 路网图层竟然覆盖在TIFF之上
  - 由于该页面下没有区分检测框的树种颜色, 所以得有个单独的逻辑屏蔽`GG树种`(即死亡植株)的逻辑: 不参与`变化变量`的株数和面积的计算, 但要新做一个对比项`死亡植株`, 也不展示其检测框
  - 该页左上和左下两个浮动卡片背景色没有跟随系统的毛玻璃亮暗效果吗, 我看是完全不透明的.
5. `/ledger`页
  - 点击每行的`操作台`跳转的居然是`/map`, 而非相应的`地块视野`. 难道是因为昨天重构后你混乱了吗? 我知道重构之前`地块视野`其实就是一张具体的TIFF视野, 现在逻辑层级关系变了--`地块-时相-TIFFs`, 你该怎么办
  - 重构这个页面, 把它打造成类似`用户侧的数据库`: 把地块/时相/tiffs相关的表的信息都展示在这里,且可编辑
    - 搜索框那一行的右端加上多组工具按钮
      - 关于`展示模式`按钮, 支持`列表展示`(类似与现状)和`树状展示`
      - 关于`编辑`按钮 -- 所有的编辑都将联动到数据库内各表的增删改
        - 支持用户手动录入某一个`TIFF`
          - 设计初衷: 这突破了`只能通过真实推理才能看到/使用TIFF`的限制, 真正做到对用户的影像资产的`可视化`和`管理`.
          - 前置工作: 
            - 设计一个全局可用的关于`获取坐标点的行政区划`的功能: 支持输入`一张tiff`或一个`坐标点`, (如果是tiff)提取其中间坐标点, 用最低复杂度且高效的算法(如射线法)得到其行政区域信息, 输出`市-县/区-乡镇`
            - 设计一个`获取图像时间属性`的功能: 输入一个图像, (如果是tiff)看其GIS元数据, 或图像本身的元数据里面有没有类似`图片创建时间`的属性, 输出`YYYYMMDD`或none
          - 录入时支持手动修改 `市, 县, 地块名, 时相, 影像名` (当然也要在placeholder展示其默认值)
            - 市县支持下拉菜单, 其范围来自项目前端全局配置`总观视野`的省份(比如现在是`广东`)
            - 地块名也支持下拉菜单, 其范围来自数据表里检索当前默认的`行政区划ID`所对应的`地块`
          - 一旦用户确认, 就要将其写入数据库相关的表里
            - 注意`市, 县`就组合成了`行政区划ID`
        - 支持删除
          - 删除`未检测`的条目很容易
          - 如果是`已检测`的, 给足用户关于危险性的充足警告
        - 支持修改: 
          - 比如某个`地块`改名
          - 给某个tiff换新路径: 即把新路径追加到`multisource_path_versions`列里的字典里
          - ...
        - 上面我描述的是功能, 至于前端视觉如何呈现, 靠你了, 根据业内最佳实践来设计吧
          
    - 树状展示: 多层级中间节点`市 -> 县 -> 地块 -> 时相 `, 叶子节点则是`tiffs`.
      - 每个中间节点支持手动展开与合拢
      - 叶子节点的开合已经实现了,保持即可. 但属性需删除`地块`, 且`状态`属性里的`已完成`啥意思,来自哪里? 换成`已检测`会不会更好理解一些
      - 如果有的叶子节点缺乏中间节点信息(比如用户未指定且系统自动获取不到的), 那每一级都支持名为`未知`的节点名.
    - 列表展示: 类似与现状, 但要列属性顺序是:`市,县,地块,时相,影像名,运行ID,状态,面积,株数,检测时间,操作`, 缺省的置空就行.
6. 行政区划ID 格式小修改: 也是市县, 但把`湛江_徐闻`改为`湛江市_徐闻县`
7. 上述一切都是我的设想, 你不能无脑执行, 而是先评价, 给建议并改善. 在执行前, 熟读`AGENTS.md`

理解透上述问题后, 先做个详细且完美的计划方案吧

## 强化'地块-单图'视野,重构'时相对比'模块
1. 搜索框下拉时，当前是`地块在上,地名在下`, 可以反过来吗?
2. `src/forestds/geo_*.py`在`src/forestds/`目录下, 这在结构设计上优雅吗?
3. 改进`/map`
	- `总体核查进度`卡片重构, 要有:
		- 子块1: `已跟踪项目`, 表示已入库的情况: 要有`项目数`(即地块数) \ `影像数`(即入库的总tiff数量) 再带一个有效影像(表示tiffs里路径真实可用的影像数) \ `时相数`(去重后的时相数量) 
		- 子块2: `已检测项目`, 表示已推理的情况: 除了`子块1`涉及到的维度, 还要有`总株数, 总面积`...
		- [我可能考虑的还不够周全, 你帮我完善, 然后以非常优雅的代码写出有设计感的卡片布局]
	- 所有入库且路径有效的tiff都应有`倒水滴坐标标注`对吧: 已检测的偏绿, 否则偏黄. 相关组件的视觉美感要同步, 比如hover它时的统计浮窗的设计.
	- 当前数据库里已有两个尚未检测的影像, 但该页提示`2个地块缺少空间坐标`, 这意思是需要tiff影像的中点坐标吗, 如果是, 那难道`tiff表`里没有`中点坐标属性`? (如果涉及到数据表model的修改, 你不要考虑兼容旧的, 直接删旧数据库) 
	- 更近一步: 我想改进`tracts表`
		- 既然有了`boundary_geom`列, 那再加一个`boundary_geom_cent`用来存地块的中心点, 方便未来可能的`/map/[tract_id]`请求, 这是个好主意吗?
			- 它的产生逻辑是: 默认下从`boundary_geom`计算而来, 否则回退到`none`, 且每当`boundary_geom`更新, 它都会自动重算
		- 再加个`effective_geom`列怎么样, 让系统`支持用户手工划定有效区域`(TODO), 这列就存这个有效区域
4. 改进类似`/map/phase_4bf1c20eacf7`路由为用户友好的`/map/[tract_id]/[phase_id]/[tiff的file_name]` 路由(或`/map/[tract_id]?phase_id=[phase_id]&tiff_name=[tiff的file_name]` ,哪种形式更好).
5. `/map`的子路由应该分`地块视野`(`/map/[tract_id]/[默认最新时相]`)和`单图视野`(见上面`第4点`)了
	- 新的`单图视野`逻辑: 沿用旧代码里的`地块视野`逻辑
	- 如果一个地块里只有一个tiff, 那这俩视野等价
	- 新`地块视野`背后的逻辑有几个重要的变动:
		- 直接加载地块内所有能使用的`tiff`
			- 要考虑到当tiff太多,同时加载导致系统顶不住,用户体验变差的情况
			- 你先根据你的经验告诉我该情况是否真的顶不住? 如果顶不住, 该设计什么样的巧思?
		- 左下角的统计浮窗也是基于当前地块加载的所有影像的整体来统计, 是单图视野的升级版.
	- 关于时相对比, 在现有时相对比模块的基础上: 
		- `单图视野`的时相对比继续保持
		- `地块视野`: 加入一个地块的两个时相都分别有10个超大tiff, 那做`地块级别`的时相对比会不会给机器带来很大的压力影响用户体验? 如果是, 那就在地图窗口右侧增设一个浮窗, 里面画个文件树: 两个中间节点代表两个时相, 下面列出其所有tiff, 让用户最多同时只能勾选`n`(你觉得几个合适, 2个,还是4个...?)个tiff来加载,以做时相对比. 用户可随时更换,只要确保`勾了就加载, 取消勾选则释放`就行). 但两个时相的所有单图的检测框要全量加载, 这个不费太多资源对吧?  [我的这个思路是业内最佳实践吗,评价并改善]
6. 改进`/dashboard`:
	- 数字卡片参考上面`总体核查进度`里面提到的维度, 但不要照搬, 你来定夺
	- `地块核查表`卡片: 要确实以`地块`为单位来统计哦, 
		- 要新增`时相数, 影像数`列
		- 考虑到一个地块的最新时相(是的,默认用最新时相)有多个tiffs, 那么株数, 密度, 覆盖率, 冠幅和... 都是这些tiffs的整体计算才对. 
		- 注意地块的超链接, 是去到新的`地块视野`哦


### 1

1. 修改路径:
	- `/map` 是总观视野, 即指定省份
	- `/map/:市/:县/:tract_id/:phase_id/` 才是地块视野
		- 该视野的左下的卡片仿照`单图视野`的样子, 但统计的是当前地块当前时相下所有`已检测`tiff的整体情况
		- 右中放一个卡片, 用可折叠的文件树形式展示所有时相(中间)和单图(叶子)
			- 只要勾选了一个时相, 其他时相自动折叠且变为浅色, 不准勾选其下的单图;
			- 激活的时相内, 支持用户随意多选单图, 选中的表示要一起加载. 
			- 卡片的左边界可以拉动以变大变小
		- 右上保持和`总观视野`一样的工具栏
		- 左上保持`单图视野`的`显示检测框`的小卡片
	- `/map/:市/:县/:tract_id/:phase_id/:tiff_name` 是单图视野

2. 修改这个全局的`判断和转换COG`的逻辑了: 只准读tif(f)格式的单文件，且收紧限制--只要它不是严格的COG，一律转为COG。
	- 后端使用它时，要有完善的日志，包括进度条；
	- 前端能用到它的地方有哪些，你帮我找下，如果是只在`访问单图视野`的预处理里用到，那就改成仅这两处才用到
		- 用户主动`录入TIFF`的场景: 就放在那个自动`读取属性`的环节，隐藏`规范路径`（用户不关心这个），增加`TIFF类型`(枚举为：normal TIFF, tiled TIFF, tiled TIFF with external overview, COG),只要不是COG，则唤出`是/否转为COG的按钮`(用`圆圈info小图标`给用户提示：为缓解内存占用，强烈建议转为COG（Cloud Optimized GeoTIFF）)，转换时给用户看进度条，结束后给用户看它的新路径（原始系统路径格式哦），并小字提醒用户可以自行删除原始tiff和tif.ovr等，`影像名`输入框的预设值也变成`***_cog`样式了。这样的效果是确保手动入库的图都是COG了。
  		- TIFF被动入库的场景：tasks页面执行推理时，不显式让用户做转换,保持在后端推理流水线里的隐式执行, 但如果执行过转换, 那入库的路径需为`*_cog.tif`哦, 且在所有推理结束后, `展示成果`的按钮前, 提示用户可以删掉旧.tif和.tif.ovr

### 2
1. 关于COG
	- 前端走到`是否转换COG`的步骤时: `暂不转换`按钮完全无效啊. 是不允许`非COG`入库吗? 请设置为允许任何tiff类型都能入库
	- tiffs增加一列`tiff_type`, 枚举: normal, tiled, ext_ovr, COG
	- COG是用来被项目内的瓦片服务器处理, 快速转成瓦片URL提供服务是吗?
	- 如果是, 那瓦片服务器能像处理COG一样处理`tiled TIFF with external overview`吗? 如果能, 那就改`检测和转换COG`工具的逻辑为: tiff_type是`ext_ovr, COG`时不需转换. 
	- 推理流水线的`检查和转换COG逻辑`又得改: `ext_ovr, COG`时不需转换
2. 我已经把旧数据库删了, 你放心修改:
	- tract_phases: 删掉`city	county	town	coverage_status	created_at`
	- tiffs: 
		- 列`ootprint_geom`和`ootprint_bbox`到底啥意思, 有没有派上用场? 没用的话我想删了
		- 你以为我记录中心坐标是为了好玩吗? 你把它存成`center_lng/lat`真的派得上用场? 为啥不直接整个坐标点对象? 我发现`单图视野`的路径下, 地图并不能进入单图视野
		- 既然在录入TIFF后, 列`orner_hash_input, rs_epsg, rs_wkt, geotransform, gsd, geo_area, area_unit, nodata`都没有被写入内容, 那你还留这些列有啥用? 也许其中某些在未来是真的有用处, 那你倒是给它添加上相应内容啊. 另外评估每一个列, 确认真的真的没用的才删掉.

### 3
1. `/ledger`的列表展示里再加一列：`TIFF状态`吧, 并依你所言`另做一个显式转换入口`；`面积`列的呈现要复用`m²->hm²->km²`的动态逻辑
2. 我尝试进入`/map/湛江市/麻章区/湛麻大块/20260703/Q20`(原图像2G, tif.ovr是700MB), 发现内存占用从5G突增到13G的峰值,之后在各级缩放中在9-13G之间跳, 我不理解:
	- 既然用了瓦片服务器, 且每张各层级的瓦片size都是KB级别的,为啥初始加载到`单图视野`(即浏览器窗口内呈现该TIFF的全貌)就要吃掉这么大的内存, 让我感觉是把2-3倍的原图塞进了内存?合理吗?
	- 全貌展示时,内存占用在接近13G, 缩放到最大(24级)则来到10G. 我就纳闷了, 瓦片的像素size都是一样的, 不管你怎么缩放, 窗口内都只能呈现相同数量的瓦片数, 我理解的对吗, 如果没错, 那为啥会有整整3G的变化
3. 你还记得`工具条`里的布局吗, 类似`+ 24.0 -`和其他4个图标, 我想改为`[像元尺寸(米)] 24.0 + -`:  也就是实时展示当前缩放级别下的单像素代表的物理边长, 这种持续实时展示会有计算压力吗, 有负担的话就别做. 另外我发现`测面积`工具不支持小于`0.1m²`的计算啊,竟然简化成了0m².

答:
- 关于你看到的 9-13G 波动：浏览器窗口瓦片数量大体固定这个理解是对的，但服务端不是只持有最终 PNG。每个瓦片请求会打开 TIFF/OVR、做重投影、采样和颜色拉伸；初始全貌往往请求低层级大范围瓦片，GDAL 可能读取较大的overview block 并缓存，多请求并发时缓存和 warp buffer 会叠加。放大到 24 级后单瓦片对应源图窗口更小，反而可能比全貌低。
- 我会把瓦片服务默认并发压到 2，并限制 GDAL cache/warp 内存；这样会牺牲一点首次加载速度，但能明显减少内存峰值。

## 优化用户体验--前端瓦片渲染

### 1
1. 听你的,不再考虑前端COG直读. 继续你刚才的缓存管理系统吧, 现在做到什么程度了, 目录至高20GB做了吗,这个20G做进配置文件了吗; 当前端新进入一个单图视野时, 后端是将所有缩放层级一并切割并缓存,还是按需来的,怎么才能有最佳的用户体验? 你之前提到瓦片的首次加载逃不掉数秒的延迟, 那有更好的优化方法吗(比如瓦片是一张张渲染出来的,这意味着串行对吧,那可否在用户缩放到的当前窗口内的瓦片们并行渲染? 当你思考这个问题的时候, 要先给出你的想法,给我审查后才能编码哦,不能擅自编码).
2. `/ledger`的列表展示的`TIFF状态`, 当出现`转COG`按钮的场景, 希望用户点了后能看到优雅的进度条; 转换完成后, 不仅数据库有相应更新, 前端的这个状态也得自动更新

答:

我的设计建议如下，先给你审查，不编码。                                                                                                                                                                          

  缓存管理系统

  我建议加这些配置：

  tiles:
    cache_enabled: true
    cache_max_gb: 20
    cache_trim_interval_s: 60
    render_concurrency: 8
    gdal_cache_mb: 640
    warp_mem_mb: 320

  - 每个瓦片按需生成，写入磁盘缓存。
  - 命中缓存时更新访问时间或维护元数据。
  - 后台清理任务按 LRU 删除最久没访问的瓦片。
  - 超过 20GB 开始清理，清到 18GB 停止，避免反复触发。
  - 清理不能阻塞瓦片响应，最多后台异步执行。
  - 增加一个轻量接口给前端/调试用：查看缓存大小、文件数、上限、最近清理时间。

  最佳用户体验

  我不建议进入单图视野时全层级预切。原因是 2G 影像全层级切完可能非常大、非常慢，而且用户可能只看几块区域。

  更好的方案是三层：

  1. 实时按需渲染兜底
     用户看哪里切哪里，保证随时可进入。

  2. 当前视口预热
     单图视野进入后，前端把当前视口 bounds、zoom 发给后端，后端后台优先预渲染：
      - 当前层级可见瓦片；
      - 当前层级周边一圈；
      - z+1 或 z-1 少量相邻层级。

  3. 用户停留/缩放趋势预取
     当用户停止移动 300-500ms 后，再预热当前区域。不要在拖动中疯狂预热。

  这样首次仍可能有少量延迟，但用户真正要看的区域会越来越顺。

  首次加载还能优化的点

  比单纯调大并发更有效的是：

  - 避免每张瓦片重复 inspect_tiff_format；
  - 缓存 TIFF 的 CRS、bounds、band、dtype、nodata、显示拉伸参数；
  - 不要每张瓦片重新做 percentile 拉伸，改成影像级预计算显示范围；
  - 对无透明需求的正射影像优先输出 JPEG/WebP，而不是 PNG；
  - 增加 singleflight：同一瓦片被重复请求时，只渲染一次，其它请求等结果；
  - 保持并发有限，比如 4-8，不要无限并发，否则只是把慢变成内存峰值。


### 2 瓦片服务器的缓存继续优化
1. 确认 /map/:市/:县/:tract_id/:phase_id 页面逻辑已经完全按`地块视野`设计了对吧
2. 访问`/map/湛江市/麻章区/湛麻大块/20260703/Q20`, 一进去就快速放大的24级, 等了约60秒, 视口内的瓦片才一个一个地渲染出来, 是不是挺离谱的? (另外监测到启动项目前内存占用4.5G, 启动后并去到24级的TIFF后, 最高占用6.4G然后回退稳定到5.2G, 这是正常的对吧)
3. 录入TIFF`E:\百度网盘下载\Q12\Q12.tif`, 其所在目录是" ray@lu-wsl  /mnt/e/百度网盘下载  ll Q12
total 1.5G
-rwxrwxrwx 1 ray ray  646 Jul  3 02:01 Q12.prj
-rwxrwxrwx 1 ray ray   90 Jul  3 02:01 Q12.tfw
-rwxrwxrwx 1 ray ray 1.1G Jul  3 02:24 Q12.tif
-rwxrwxrwx 1 ray ray  134 Jul  3 02:03 Q12.tif.aux.xml
-rwxrwxrwx 1 ray ray 407M Jul  3 02:06 Q12.tif.ovr", 自动检测到`TIFF 类型：tiled TIFF with external overview`, 可是填充的市是`中华人民共和国市`,县是`未知县`,乡镇是`未知`, 这不正常吧. 是图像本身的问题,还是代码逻辑bug? 它应该像Q20一样准确识别行政区啊.
  - 另外: 市县乡输入框内部右端能给个下拉选项吗? 范围限定在前端配置`overviewRegion`(或环境变量)里.
4. `E:\百度网盘下载\Q18\Q18.tif`甚至读到"读取状态：可读取
TIFF 类型：invalid", 可是它" ray@lu-wsl  /mnt/e/百度网盘下载  ll Q18
total 4.6G
-rwxrwxrwx 1 ray ray  646 Jul  3 04:21 Q18.prj
-rwxrwxrwx 1 ray ray   90 Jul  3 04:21 Q18.tfw
-rwxrwxrwx 1 ray ray 3.3G Jul  3 05:22 Q18.tif
-rwxrwxrwx 1 ray ray  468 Jul  3 04:25 Q18.tif.aux.xml
-rwxrwxrwx 1 ray ray 1.4G Jul  3 04:40 Q18.tif.ovr", 谁的问题?

### 继续优化瓦片加载
1. 你说`当前 shell 没有高德 key`, 可`.env`里的 AMAP_WEB_SERVICE_KEY 不能用吗
2. 你说`选项来自当前资产表已有行政区`, 哪里来的资产表,我怎么不知道, 难道就只是哪个地级市的列表吗? 如果是,这也太鸡肋了, 随便选中一个市, 县乡的下拉都是空的啊. 为啥不做`外部行政区全量库`, 我个人认为这样会更专业和方便修改,你说呢?
3. 继续完成`地块视野成品`
4. 关于提升瓦片加载的用户体验, 按你的计划优化吧
5. 那像`Q18.tif`的情况, 读取属性后的`读取状态：可读取`换成简化的报错信息(如`TIFFReadDirectory: Failed to read directory at offset 3439020906`)是不是更好
6. 当我表里有`湛江市
麻章区
麻章大区
20260703
Q20
湛江市
麻章区
麻章大区
20260703
Q21`两个图, 进入地块视野, 发现右边浮窗里只勾选了Q20, 这是正常的对吧? 但当我手动去勾选Q21, 地图上并未出现其图层(单独勾选和两个都勾选的情况是一样的), 咋回事?
7. 总观视野下, Q20和Q21在地理上很近导致俩坐标点重叠, 这种情况我就无法hover到Q20, 有办法让两个点错位排列吗 (但zoom in 到两点足够远时就恢复正常)
8. 当来到单图视野, 为啥左下角的统计卡片是`地块的`而非以前还没区分`地块和单图`时的统计逻辑?
9. 当在`/tasks?input_path=%2Fhome%2Fray%2Frays%2Frepos%2F4estDS%2Fdata%2F多源数据研究_徐闻%2FDOM%2Fxuwen_big_cog.tif&tract_id=湛徐大块&phase_id=20260710`点击`开始推理`时:"Failed to load resource: the server responded with a status of 500 (Internal Server Error)
chunk-NS2365CB.js?v=63851b10:1248 Warning: [antd: message] Static function can not consume context like dynamic theme. Please use 'App' component instead."

### 2 解决'监管台账'bug
1. 你做了`把自动识别从“中心点一次”升级成“中心+四角投票”`, 可是为啥一个准确的点坐标怎么会出现反查不到行政区信息的呢? 是你算法有问题,还是高德有问题
2. `E:\百度网盘下载\Q12\Q12.tif`读取属性时依然报`未知市县乡`
3. 再次推理 xuwen_big_cog, 得到`请求失败 (503): 推理队列不可用，请确认 Redis/worker 已启动: Error 111 connecting to localhost:6379. Connection refused.`

### 3 debug
1. 总观视野下, 此前完全重叠的坐标现在隔得十万八千里, 很离谱啊! 我只想错开一点点的那种, 够我hover就行的
2. 推理后`/ledger`奇怪的现象: `湛江市	徐闻县	湛徐大块	20260710`的推理执行完, 表里原来的那行被替换成了两行:
  - `湛江市	徐闻县	湛徐大块	20260710		不可用	`, 路径为空, 且推理状态`未检测`(浅灰色,无超链接); 操作列没有删除按钮
  - `未知市	未知县	湛徐大块	20260710	xuwen_big_cog	COG · 可瓦片	已检测	b59ad9	10.262 hm²	1,530	2026/7/11 06:06:45`; 当点击`操作台按钮`能正确跳转单图视野,但左下角统计卡片除了面积全都没有值, 图层只有检测框, 没有tiff, 同时内存占用剧增7GB,要知道这个图是COG且只有570MB
  - 同时树状展示中相应地块的`20260710`时相下只显示一个叶子: `未知影像 未检测`
总之问题很大, 解决吧

### 4 debug

1. 总观视野下那些重合的坐标点现在互相距离不那么远了，但还是有点远，再靠近吧，让用户直观感受到它们实际就是物理上特别近，但能确保hover到。另外坐标图标现在是黄绿双色区别有无落实推理对吧，优化下：
  - 黄绿两色都浅一点
  - 把图标的轮廓线颜色用以区分`所属地块`（地块与这个轮廓颜色没有固定的映射关系，只要相邻的地块颜色不同就行）
2. 我把`Q20`推理完成，进入其单图视野后，系统内存从5G剧增到12G，同时出现超级多的` 
GET http://localhost:5173/api/v1/tiles/tiffs/20260703/Q20.tif/19/422703/230740 404 (Not Found)maplibre-gl.js?v=63851b10:278  
GET http://localhost:5173/api/v1/tiles/tiffs/20260703/Q20.tif/19/422709/230740 404 (Not Found)maplibre-gl.js?v=63851b10:278  
GET http://localhost:5173/api/v1/tiles/tiffs/20260703/Q20.tif/13/6604/3605 404 (Not Found)client.ts:46 
POST http://localhost:5173/api/v1/tiles/tiffs/20260703/505c5/preheat 404 (Not Found)maplibre-gl.js?v=63851b10:278 
GET http://localhost:5173/api/v1/tiles/tiffs/20260703/Q20.tif/12/3302/1802 404 (Not Found)`，且tiff图层无法加载。
  - 啥情况，瓦片服务器在这个图上失效了吗？
  - 另标注框图层（有8790个框）也是等了十几秒才加载 -- 我个人理解它不应该是秒加载吗？如果是，那这么慢是因为被谁阻塞了吗，要不要考虑把加载它的优先级提前（当然这个设计非必需，如果不复杂就做）
3. 单图视野（`/map/湛江市/麻章区/麻章大区/20260703/Q20.tif`）和地块视野（`/map/湛江市/麻章区/麻章大区/20260703`）的左下角的统计卡片里`覆盖率、冠幅和、冠尺寸、冠面积`都是空的
  - 另外冠尺寸/冠面积的展示格式改为：
    - 不要呈现`宽x高`，只要一个值`等效宽高`
    - 保持简约优雅和亮暗适配：示例`冠尺寸(m): 分布 0.25±0.30; 分位 【0.09 - 0.13 - 0.28 - 0.74 - 1.58】`
      - `分布`和`分位`两个词用得好不好？或者你给出更专业的两字的词来替换掉。然后分别用带不同颜色的浅色填充的文本框
      - 每个数字都支持用户hover，分别注释：均值，标准差，极小值，p25，中值，p75，极大值
      - 冠面积和树高（当存在时）也都类似设计
4. 地块视野里，右中的`影像文件`卡片：取消树状，只列出当前时相的tiffs。卡片默认宽度收窄。
5. 台账里列表展示已检测的行的左侧加号点击后会展开一片关于文件树的区域对吧， 需要在该区域右上交加个按钮唤醒之前设计好的`导出`按钮：按一次，文件树全展开且带复选框；再按一次回到文件树初始状态对吧。

### 5 [agy] debug
1. 地块/单图视野:
  - 左下角卡片:
    - 如果是单图视野时, 标题应该是`地块名 - 影像名`
    - `冠面积(m²)`被显示成两行了, 是你给它的单元格宽度太窄了吗, 多给一点点吧
    - `覆盖率`改成`种植密度`
    - 卡片右边缘支持拉动, 所以卡片内也要支持左右滚动
  - 右中卡片改到右下角
2. `监管台账`页面树状展示时, 每个叶子节点/时相节点右侧的超链接按钮太多了,视觉不友好, 把文字全删掉,只保留图标
3. `tasks`页面, 左侧下边的`历史作业`卡片, 删掉标题, `作业`改为`历史作业`
4. `/map`页面, 水滴图标的轮廓再粗一点, 且颜色不应与图标内填充的黄/绿色冲突

### 6 [codex] debug

1. `ledger`页面, 导入`xuwen_big_cog.tif`时的`影像名`没有后缀, 执行推理后, 后缀就出来了, 解决这个bug
2. `change`页面问题非常大:
  - 进入后10秒内产生了2000个`maplibre-gl.js?v=63851b10:365 Error: AbortError
    at MapLibreController.setRasterOverlay (MapLibreController.ts:268:51)
    at syncSide (TemporalWipe.tsx:200:32)
    at TemporalWipe.tsx:125:5`
3. `map`页面
  - 左上角搜索图标右侧加个工具组件(两列的): 
    - 先是一个下拉框, placeholder写`选择进入项目`, 下拉呈现当前数据库内所有的地块, 用户选好后就能跳到对应的`地块视野-最新时相`
    - 后是一个按钮,名为`单时相`, 默认为开(表示每个`地块`只能展示最新时相的坐标图标), 点击后表示切换为多时相模式(每个`地块`展示所有坐标图标)

### 7 [gpt5.6] debug
1. 不对啊, 我让你去掉任何情况下的影像名后缀,你怎么又给我加上了
2. 删掉`“选择进入项目”下拉框`,我不打算要了
3. `单时相`按钮只在`map`页面(总观视野)才有作用对吧? 为啥其他视图还有它. 另外把它优化得跟现有的浮块/组件风格和谐
4. 单图视野和地块视野左上角的`时相对比`按钮是给我删了吗? 
5. `/change?tract_id=湛徐大块`页面
  - 里左侧的检测框加载得比较快, 右侧的我等了一年都没加载出来, 什么原因, 怎么优化
  - 左侧`时相对比`和`变化量化`量卡片都要收窄, 且做成一样宽.
  - 左上卡片
    - 删掉底部那行:`n个时相`
    - 把`单图视野`里的`显示检测框`卡片做成这个卡片里的一个子模块, 且列出的树种得是正在对比的两个时相的整合. 注意两个时相的框的颜色不能一样, 不同树种的框的颜色也不一样. 
6. 当点击进入`Q20`单图视野, 有8790个标注框, 框的图层硬生生等了30秒才加载出来! 这是为什么, 能优化吗

### 8 debug
1. 当后端启动那一刻, `configs/default.yaml`里的配置就定死了吗? 因为我在运行过程中修改了里面的模型为`m03`,但推理时日志发现仍然在`模糊搜索权重 'm07'`
2. `.venv/bin/dramatiq`是什么? 是推理后产生的吗? 我发现推理后它就死死占住大量内存空间!
3. `/change?tract_id=****`
  - 底部中间滑块卡片, 有3个可滑动节点时, 左点能滑动, 右点滑不动,怎么回事
  - `显示检测框`处, 多个树种怎么是横向排列呢, 不应该纵向吗

### 9 debug
1. 项目里异步任务 worker就没有终止条件吗? 我现在面临的情况: 只推理一个图片任务, 结束后dramatiq就一直常驻着, 这肯定不合理对吧！给出你的最佳实践的方法。
2. 时相对比页面的滑块的用户交互做的太差, 我把右端滑倒中间点, 它自己又跳回去了. 我想要真正的滑动, 而非跳动, 而且对用户释放鼠标的时机的容错很高才行. 滑杆也要粗一些
3. 删除一个已推理的TIFF相关的所有数据，需要双层确认。在第二次确认时出现`请求失败 (409): 将移除 1530 株推理结果、观测和相关运行记录，再次确认！。该操作不可恢复。`， 控制台显示`Failed to load resource: the server responded with a status of 409 (Conflict)`， 这啥意思？另外这个报错并不影响我删除相关内存。
4. 刚才做了一个推理，run_id=3585ce, 
  - 你看我配置文件里模型是啥, 然后此次运行用到的模型又是啥？不一样吧，为啥呀，我疯了！
  - 日志显示`单木=1530`，可是`ledger`表格显示`株数=3060`, 太离谱了吧！是因为数据表不干净吗？（因为我之前删过同影像名同时相的运行数据）

### 10 debug
1. 你刚提到`当前要立即释放内存，可在启动 Dramatiq 的终端正常 Ctrl+C 停掉 worker`， 如果把这个逻辑做成一个按钮放到`作业队列`区域的右上角，你推荐吗？推荐的话就做吧



## 智能复核模块

项目到目前已经很好了. 现在我有新设想: 增加`划定有效区域`和`智能复核`两个功能
1. 功能1: 划定有效区域
  1. 背景: 正射影像肯定是把真实种植区和无效的水域/道路/杂草灌木...都包括了的，这势必造成`面积`的失真和模型的`假阳率增高`, 所以得支持用户`创建新的有效区域掩膜矢量文件/对象`或`修改已有的掩膜` 
  2. 角色定位: `有效掩膜`是与`地块`唯一绑定的, 所以它能被用于对应地块下的所有时相和所有tiffs。tracts表里的`effective_geom	boundary_source`就是为它准备的
  3. 功能设计: 
    a. 入口: 最佳的位置、呈现方式是？
    b. 具备一个业内专业的矢量编辑功能
    c. 关于导入：应支持用户导入矢量文件，也支持从tracts里面拿
2. 功能2: 智能复核
  1. 背景: 现有的推理模型可能在复杂场景和多类树种的覆盖上没那么理想, 可能会在用户的新图上出现不少检测框的误判/漏判，所以打算用`支持文本prompt+视觉prompt`的多模态模型（如 yoloe-26x等，可拔插）来做智能复核. 
  2. 角色定位：深度参考网站`https://trexlabel.com/workspace/`的各个页面和功能的设计，不同的是：
    - 我们的图不是用户上传的小jpg/png等，而是加载在卫星底图上的超大tiff图层（可能不止一个tiff）
    - 我们的导出不是把检测框做成COCO/YOLO格式的标注文件，而是直接入库，或导出为可供其他GIS软件使用的矢量文件
    - ……其他
3. 上面只是我作为一个非专业人士的个人想法，而你是业内顶级的架构师和前端工程师，功能点如何敲定，前后端分别如何设计如何协同，技术栈如何选择……都由你来定夺，确保高效，优雅，无性能瓶颈，符合业内最佳实践。开始吧，形成一个非常详细的实施方案给我看看。
  
    












## 将做但暂时不做的

### 给[agy]的细节修理
1. 地块/单图视野:
  - 左下角统计卡片里`冠尺寸/冠面积/树高`里的`分位`里的`中值`: 字体加粗且颜色不同于其他4个分位值, 注意要亮暗模式协调.
  - `显示检测框`的卡片宽度收窄, 只比内部的那个按钮大一点
2. `tasks`页面
  - `历史作业	状态	时间`这3列不应该等宽, 而是自适应, 特别是时间列, 不要显示秒, 且不要折行
3. 没有做亮暗模式适配的地方：
  - tasks页面二次确认`释放worker`时，
  - ledger页面删除已推理的数据的二次确认时。
  - `历史作业`卡片内的滚动条


### 给[codex]的优化
1. 时相对比时
  - 只渲染较新时相一边的tiff? 
2. 
3. 

